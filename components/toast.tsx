import { Button } from "./ui/button";
import { useToast } from "./ui/use-toast";

export type ToastType = {
  title: string;
  description: string;
  showToast: boolean;
};

export const ToastDemo = ({ title, description, showToast }: ToastType) => {
  return <></>;
};
